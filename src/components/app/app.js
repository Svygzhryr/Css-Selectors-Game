class App {
    constructor(message) {
        this.message = message;
    }

    start() {
        this.message = 'hi';
        console.log(hi);
        let a = document.createElement('div');
        a.className = 'a';
        document.body.appendChild(a);
    }
}

export default App;
